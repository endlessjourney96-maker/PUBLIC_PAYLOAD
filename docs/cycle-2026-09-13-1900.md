# Integrated autonomous cycle — 2026-09-13 19:00 JST

## Critical path
Production runtime identity -> smoke QA -> anonymous measurement loop -> first observed funnel KPI.

## Verified this cycle
- Canonical `OPERATIONS.md` was re-read from the default branch.
- GitHub default-branch HEAD is `3b32e960ef696c18683181c6f2601e04018d809b` (`docs: add minimal production runtime verification card`).
- The previous GitHub-unavailable blocker remains obsolete.
- Repository code alone still does not prove which commit is live on Cloudflare; do not infer production parity.

## PM decision
- Primary MVP remains highest priority.
- New business scout: **no new start** this cycle. No candidate has evidence strong enough to justify delaying production/CV validation.
- Rakuyoko, note/content, AEO and corporate DX remain parked/reuse candidates until the funnel is measurable.
- Do not add catalog/UI features before runtime identity and smoke QA.

## KPI
No new observed public KPI in this cycle.
- PV: unobserved
- diagnosis_start: unobserved
- diagnosis_complete: unobserved
- recommendation_click: unobserved
- CV: unobserved
- revenue: unobserved

## Blocker
Missing read-only production facts from the existing Cloudflare project:
1. Production URL
2. Connected GitHub repository
3. Production branch
4. Deployed commit SHA
5. Auto-deploy enabled/disabled

No Cloudflare setting change is authorized or required to read these values.

## Next action
1. Obtain the five read-only runtime facts above through an already-connected tool if one becomes available, otherwise owner read-only confirmation remains the minimal gate.
2. Compare deployed SHA with GitHub HEAD.
3. If parity: execute smoke acceptance (`/`, landing CTA, mobile CTA, diagnosis completion, recommendation click, buy-nothing path).
4. Only after runtime architecture is confirmed, implement the smallest compatible anonymous `/api/events` collector/readout and tests.
5. Record only observed KPIs.

## Guardrails
No external publish/config change, account change, paid service, contract, purchase, affiliate activation, outbound sales/SNS post, or identity/bank/tax operation without explicit owner approval.
